import { TownCensus } from '../src/townCensus';
import { PopHistory } from '../src/popHistory';

beforeEach(() => {});

afterEach(() => {});

// TODO unit tests for this.coord2census sort integrity, latestCensus, etc
describe('PopHistory', () => {
    describe('todo', () => {
        test('todo', () => {
            expect().toBe();
        });
    });
});
