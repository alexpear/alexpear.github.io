'use strict';

// This file is run when building the Waffle Cards software.
// It writes cards.js. Users should edit cards.yml as the source of truth.
// cards.js will be a generated file that the software interacts with, & that browserify can bundle.

const Util = require('../../../util/util.js');
const fs = require('fs');
// const path = require('path');
const yaml = require('js-yaml');

class Parser {


    static run () {

    }
}

Parser.run();
