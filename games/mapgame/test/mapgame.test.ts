import { MapGame } from '../src/mapgame';

// TODO mock L, quickUpdateScreen()

beforeEach(() => {
    // set clock to a fixed time.
});

afterEach(() => {

});

describe('MapGame', () => {
    describe('todo', () => {
        test('does todo', () => {
            // TODO: Visit a 'home' location, move GPS location 0.01 degrees south, visit again. Move back home. Check that both those locations now have zero points. 
            //TODO then set time to after the next midnight. Visit home. Check that home has 0 points and south location has 1 point.

            // expect().toBe();
        });
    });
});
